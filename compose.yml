services:
  go-server:
    image: index.docker.io/kortuga/bear-trak-backend:latest
    restart: always
    build:
      context: ./go-server
      dockerfile: ../docker/go/Dockerfile
      platforms:
        - "linux/amd64"
    ports:
      - "3000:3000"
